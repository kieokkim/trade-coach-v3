Part B — TradeCoach 로드맵

ROADMAP_TRADECOACH.md로 저장 권장

0. 이 프로젝트의 좌표
항목	값
사용자 1명	본인 (ICT 트레이더, 봇으로 실행)
대체재	없음 — 지금은 복기 자체를 안 하고 있음
차별점	거래 하나하나가 아니라 반복 패턴을 짚음
검증 방식	봇 실거래 손익 = 자동 ground truth (실거래 전환 후)
끝의 정의	1차: 본인이 매주 쓴다(L1). 2차: 유튜브 경로로 외부 사용자(L4·L6)
면제	L5 자율 (복기는 궁금할 때 여는 도구)
Phase A — 블로커 해소

목표: L7 방어. 지금 상태로는 아무것도 보여줄 수 없다.

STEP2-C live e2e 검증 ← 최우선, MVP-C 블로커
실 API 키로 journal_entries INSERT 확인
손절규율 3치(pass/fail/미채점) 실제 동작
API 키 오류 시 명시적 에러 노출 (조용한 실패 금지)
Upbit 캔들 경로 수정
fetch_candles가 예외를 삼켜 [] 반환하는 문제
"조회 실패"와 "데이터 없음"을 구분해야 함 (STEP2a에서 다른 경로엔 이미 적용된 패턴)
TC-D47 커밋 (MVP 범위 확정, 합의만 된 상태)
unpushed 커밋 정리 후 push

⚠ 2번이 특히 중요한 이유: 나중에 외부 사용자가 열었는데 캔들이 빈 배열로 와서 화면이 비면 그 사람은 다시 안 온다. 데모는 재시도가 없다.

Phase B — 검증 (L2, 이 프로젝트의 병목)

전제: 자동매매 실거래 전환

목표: A+ 스코어링 기준이 실제로 유효한지 순환논리 없이 확인

봇 거래 로그를 TradeCoach가 읽는 경로 구축
TradeCoach가 A+로 판정한 진입들의 실제 손익 추적
A+ / 非A+ 간 성과 차이가 있는지 측정
차이가 없으면 → A+ 기준 자체가 무의미. 재설계 대상
차이가 있으면 → 순환논리 없는 최초의 검증. 이게 이 프로젝트의 핵심 성과가 된다
⚠ look-ahead bias 주의 (TC-D30) — 판정 시점에 알 수 없던 정보가 새어들어가면 안 됨

🎓 이 Phase가 SalesCoach와 근본적으로 다른 점: SalesCoach는 "이 반품이 이례적이었나"의 정답이 나중에도 안 나와서 사람 라벨링이 필수였다. 여기는 손익이 정답이라 기다리면 데이터가 쌓인다. 같은 검증 문제가 도메인에 따라 얼마나 다르게 풀리는지의 대조 사례 — 포트폴리오·면접에서 쓸 수 있는 서사다.

Phase C — 본인 사용 (L1)

목표: 본인이 매주 실제로 연다.

프론트 목업 v7 기반 Streamlit 구현 (설계 90%+, 구현 0%)
주 1회 복기 루틴 확립
코칭 결과 → 봇 파라미터 조정 → 결과 확인의 한 바퀴 완주

완료 판정: 4주 연속으로 열었다. 안 열었으면 제품이 아직 쓸모없다는 뜻이고, 그 이유를 찾는 게 다음 작업.

Phase D — 외부 배포 (L4·L6)

전제: Phase B에서 A+ 기준이 검증됐을 것. 미검증 기준으로 남을 코칭하지 않는다.

경로: 유튜브 ICT 채널

커뮤니티 직접 진입이 아니라 채널 운영자를 먼저 설득하는 구조. 즉 사용자 1명 이전에 게이트키퍼 1명이 있다.

운영자 입장의 계산: 구독자에게 소개하는 건 자기 신뢰를 빌려주는 행위다. 별로면 자기가 손해. 그래서 두 가지가 필요하다.

완성도가 부끄럽지 않을 것 (버그로 화면 비면 운영자가 욕먹음)
운영자에게도 이득일 것

각도: ICT 채널 운영자는 "시청자가 배운 걸 실제로 적용하는지" 알 방법이 없다. 영상은 일방향이니까. TradeCoach가 "당신 채널에서 가르친 셋업 기준으로 시청자가 자기 거래를 점검하는 도구"가 되면, 홍보 부탁이 아니라 콘텐츠 보완재 제안이 된다.

데모 형태

남의 거래를 넣게 하면 안 된다. 데이터 연동·계좌 연결·프라이버시 — 문턱이 올라가면 아무도 안 쓴다.

대신 미리 준비한 케이스를 보여주는 형태:

"이 트레이더는 3주간 12번 거래했고, 그중 8번이 같은 실수였습니다" → 어떤 실수인지, 어떻게 탐지했는지, 어떻게 코칭하는지

클릭만 하면 되고 30초 안에 제품이 뭔지 이해된다. "내 거래도 넣어보고 싶다"가 나오면 그게 L1 외부 사용자의 시작이다.

예상 마찰

ICT 커뮤니티는 도메인 지식이 있는 집단이라 판정이 잔인하다.

FVG/OB 탐지가 틀리면 즉시 들킨다. 무료 라벨링을 받는 셈이라 나쁘지 않지만, 첫인상은 한 번뿐이다.
"자동 감지"로 오해받으면 매매 신호 도구로 인식되고, 코치형 포지셔닝이 무너진다.

⚠ 관계자본 없는 콜드 아웃리치다. 응답률이 낮은 게 정상이고 여러 채널에 보내야 한다. 개발과 동등한 시간을 배정할 것 — "개발 끝나면 하는 것"으로 두면 계속 밀린다.

보류 (post-MVP)
A+ Setup Memory
개인화 룰북 생성
ICT 이외 rule-based 스타일 교체 가능성 탐색
Style Layer (기억 정정으로 우선순위 하락 — 본인이 이미 ICT 트레이더)
진행 판정표
Phase	상태	완료 조건
A 블로커 해소	🔶 진행중	live e2e 통과, 캔들 경로 수정
B 검증	⬜ 대기	실거래 손익으로 A+ 기준 유효성 확인
C 본인 사용	⬜ 대기	4주 연속 열었다
D 외부 배포	⬜ 대기	외부 사용자 1명이 자기 거래를 넣었다

B가 C의 전제는 아니다 — C(본인 사용)를 먼저 해도 되고, 오히려 C를 하면서 B의 데이터가 쌓인다. 다만 D는 반드시 B 이후다.